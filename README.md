# 座位表调整工具

一个用于生成和调整学生座位表的图形界面工具，支持拖拽调整、数据导入导出等功能。

## 功能特点

- 🎨 **可视化座位表**：直观显示学生座位分布
- 🎯 **拖拽调整**：支持通过拖拽方式调整座位
- 📊 **Excel导入**：支持从Excel文件导入学生数据
- 💾 **数据保存**：自动保存座位表数据
- 📄 **Word导出**：支持将座位表导出为Word文档
- 🖼️ **图片导出**：支持将座位表导出为图片

## 依赖库

项目使用以下外部依赖库：

- pandas >= 1.0.0
- pillow >= 8.0.0
- python-docx >= 0.8.10

## 安装方法

1. 克隆仓库：
```bash
git clone https://github.com/52liulian/Seating-Chart-Adjustment-Tool.git
```

2. 进入项目目录：
```bash
cd Seating-Chart-Adjustment-Tool
```

3. 安装依赖：
```bash
pip install -r requirements.txt
```

4. 运行程序：
```bash
python main.py
```

## 使用说明

1. **导入学生数据**：点击"导入数据"按钮，选择包含学生信息的Excel文件
2. **调整座位**：通过拖拽方式调整学生座位
3. **保存数据**：程序会自动保存座位表数据到"座位表数据.json"文件
4. **导出座位表**：点击"导出Word"或"导出图片"按钮，将座位表导出为相应格式

## 项目结构

```
Seating-Chart-Adjustment-Tool/
├── main.py          # 主程序文件
├── config.ini       # 配置文件
├── requirements.txt # 依赖库列表
├── 座位表数据.json    # 座位表数据文件
└── images/          # 图片资源目录
    └── img.ico      # 程序图标
```

## GitHub仓库

项目地址：[https://github.com/52liulian/Seating-Chart-Adjustment-Tool](https://github.com/52liulian/Seating-Chart-Adjustment-Tool)

## 许可证

MIT License
